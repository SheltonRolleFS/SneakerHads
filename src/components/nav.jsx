const Nav = () => {
  return (
    <nav>
      {/* This should be a Link when React Router is added */}
      <div className="container">
        <a href="/">Browse Items</a>
      </div>
    </nav>
  );
};

export default Nav;
